# Local install
node_modules/
*.log
.DS_Store
.env
.env.local

# Build output. Phase 1: not served; CI builds to validate. dist/
# stays uncommitted so the diff is clean. When Phase 2 wires
# Cloudflare to serve from dist/, the deploy will need CF Pages'
# build command set so dist/ exists on the edge — until then,
# ignoring it locally is correct.
dist/
